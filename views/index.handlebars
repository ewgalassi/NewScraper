<ul>
    {{#each Article}}
    <li>
        <p href="{{this.url}}">{{this.headline}}</p>
        <p>{{this.summary}}</p>
        <button data-id="{{this.id}}" class="saveArticle">Save Article</button>
    </li>
    {{/each}}
</ul>